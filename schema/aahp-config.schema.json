{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/homeofe/AAHP/schema/aahp-config.schema.json",
  "title": "AAHP Config",
  "description": "Optional aahp.config.json consumed by the config-driven AAHP release gates, aahp doctor, and the verify gate's reviewed handoff-impact classifier. Every section is optional; absent configuration preserves each command's default behavior.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional JSON Schema reference for editor tooling."
    },
    "versionSites": {
      "type": "array",
      "description": "Files that must contain the current package.json version at least minOccurrences times (checked by check-version-sync.mjs).",
      "items": {
        "type": "object",
        "required": [
          "file"
        ],
        "properties": {
          "file": {
            "type": "string",
            "description": "Path relative to the project root."
          },
          "minOccurrences": {
            "type": "integer",
            "minimum": 1,
            "description": "Minimum number of occurrences required (default 1)."
          },
          "boundary": {
            "type": "boolean",
            "description": "If true, match on word boundaries so 3.3.0 does not match inside 3.3.09 (default false: literal substring)."
          },
          "note": {
            "type": "string",
            "description": "Human-readable reason this file pins the version."
          }
        },
        "additionalProperties": false
      }
    },
    "claims": {
      "type": "array",
      "description": "Capability numbers pinned to one canonical value across surfaces (checked by check-claims.mjs).",
      "items": {
        "type": "object",
        "required": [
          "id",
          "canonical",
          "advertised",
          "phrase",
          "surfaces"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Short identifier for the claim (used in messages)."
          },
          "canonical": {
            "type": "string",
            "description": "Token shown to humans, e.g. \"350+\"."
          },
          "advertised": {
            "type": "integer",
            "description": "The number every surface must carry."
          },
          "phrase": {
            "type": "string",
            "description": "Regex source; capture group 1 is the number to compare."
          },
          "flags": {
            "type": "string",
            "description": "Regex flags (default \"gi\")."
          },
          "floorCmd": {
            "type": "string",
            "description": "Optional repo-relative path to a Node script whose stdout yields an integer ground-truth floor; advertised must not exceed it. Run with the Node interpreter, no shell (a path escaping the project root is rejected). Omit for an editorial claim."
          },
          "joinTsConcat": {
            "type": "boolean",
            "description": "If true, join adjacent JS string-literal concatenations before matching."
          },
          "surfaces": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "file"
              ],
              "properties": {
                "file": {
                  "type": "string"
                },
                "note": {
                  "type": "string"
                }
              },
              "additionalProperties": false
            }
          }
        },
        "additionalProperties": false
      }
    },
    "generate": {
      "type": "object",
      "description": "Handoff generator + freshness gate (aahp-dashboard.mjs).",
      "properties": {
        "log": {
          "type": "object",
          "description": "Generate a LOG release journal from a CHANGELOG. Opt-in: omit to leave an append-only agent-journal LOG.md untouched.",
          "properties": {
            "source": {
              "type": "string",
              "description": "CHANGELOG path relative to the project root (default CHANGELOG.md)."
            },
            "target": {
              "type": "string",
              "description": "Output path relative to the project root (default .ai/handoff/LOG.md)."
            },
            "title": {
              "type": "string",
              "description": "H1 title for the generated journal."
            }
          },
          "additionalProperties": false
        },
        "freshness": {
          "type": "object",
          "description": "Check that a hand-curated doc's 'Current version: **vX.Y.Z**' line matches package.json.",
          "properties": {
            "file": {
              "type": "string",
              "description": "Path relative to the project root (default .ai/handoff/NEXT_ACTIONS.md)."
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "forbiddenPatterns": {
      "type": "array",
      "description": "Regexes that must NOT appear in tracked files (checked by check-forbidden-patterns.mjs).",
      "items": {
        "type": "object",
        "required": [
          "pattern"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "pattern": {
            "type": "string",
            "description": "Regex source. Store a literal-unicode ban as an escape (e.g. \\u2014) so the config file itself does not match."
          },
          "flags": {
            "type": "string",
            "description": "Regex flags (default \"gm\")."
          },
          "message": {
            "type": "string"
          },
          "include": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "git pathspecs to scan (default: common text files)."
          },
          "exclude": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      }
    },
    "docSync": {
      "type": "array",
      "description": "Groups of sources whose extracted value-set must agree (checked by check-schema-doc-sync.mjs).",
      "items": {
        "type": "object",
        "required": [
          "sources"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "sources": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "file",
                "pattern"
              ],
              "properties": {
                "file": {
                  "type": "string"
                },
                "pattern": {
                  "type": "string",
                  "description": "Regex; capture group 1 is one value. The set of group-1 values must match across all sources in the group."
                }
              },
              "additionalProperties": false
            }
          }
        },
        "additionalProperties": false
      }
    },
    "docLinks": {
      "type": "object",
      "description": "Internal Markdown link checker (check-doc-links.mjs). Presence enables it.",
      "properties": {
        "include": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "git pathspecs of docs to check (default: README.md, CLAUDE.md, CONTRIBUTING.md, .ai/handoff/*.md)."
        }
      },
      "additionalProperties": false
    },
    "docPaths": {
      "type": "object",
      "description": "Shape assertions over the adopter-facing documents (check-doc-shape.mjs). Presence enables it. Backticked repo-relative paths whose first segment is a tracked top-level entry of this repository must resolve against the git index; paths that intentionally describe a CONSUMER's tree are declared in adopterPaths with a reason and are reported as not assessed on every run. The gate exits 2 on anything it could not assess, so an unreadable tree never reports clean.",
      "properties": {
        "include": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "git pathspecs of documents to scan (default: README.md, CLAUDE.md, CONTRIBUTING.md)."
        },
        "adopterPaths": {
          "type": "array",
          "description": "Reviewed exceptions: paths that resolve in an adopting repository rather than in this one. Each entry pins the exact number of occurrences it covers, so a NEW mention of the same string is a failure rather than a silent exemption, and an entry that matches nothing is a failure too. The same string is often right in one sentence and wrong in the next, which is why this is a counted change detector and not an allowlist.",
          "items": {
            "type": "object",
            "required": [
              "path",
              "occurrences",
              "reason"
            ],
            "properties": {
              "path": {
                "type": "string",
                "minLength": 1,
                "description": "The exact span as it appears between backticks, without a leading ./ or a trailing /."
              },
              "occurrences": {
                "type": "integer",
                "minimum": 1,
                "description": "Exact number of reviewed occurrences across the scanned documents. Any other number is a finding, in both directions."
              },
              "reason": {
                "type": "string",
                "minLength": 1,
                "description": "Why this path is correct while naming nothing in this repository."
              }
            },
            "additionalProperties": false
          }
        },
        "requiredHeadings": {
          "type": "array",
          "description": "Headings that must exist, optionally before another heading. A missing heading is a failure; an anchor that itself cannot be found is a failure too, because an ordering that cannot be judged is not a pass.",
          "items": {
            "type": "object",
            "required": [
              "id",
              "file",
              "pattern"
            ],
            "properties": {
              "id": {
                "type": "string",
                "description": "Short identifier used in messages."
              },
              "file": {
                "type": "string",
                "description": "Tracked file to search, relative to the project root."
              },
              "pattern": {
                "type": "string",
                "description": "Regex matched line by line; the first matching line is the heading."
              },
              "before": {
                "type": "string",
                "description": "Optional regex naming a line the heading must precede."
              },
              "note": {
                "type": "string",
                "description": "Human-readable consequence, printed when the heading is missing."
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "acceptanceCriteria": {
      "type": "object",
      "description": "Input paths for the ADVISORY `aahp criteria` report (report-acceptance-criteria.mjs). This is not a gate: it is not part of `aahp check`, it has no enforcing mode, and it always exits 0. It reports legacy heading aliases, plain bullets where task boxes belong, criteria left unresolved on tasks the manifest marks done, and everything it could not read. Optional: absent, the report uses these defaults. A clean report is not proof; see README Section 8.7 for the published blind spots.",
      "properties": {
        "include": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "git pathspecs of documents to scan (default: .ai/handoff/NEXT_ACTIONS.md). List live task documents, not templates: a template task id can collide with a real task id in the manifest."
        },
        "manifest": {
          "type": "string",
          "description": "Path to the task registry that supplies task status (default .ai/handoff/MANIFEST.json), relative to the project root and required to resolve inside it. A value that escapes the root is reported as manifest-outside-root and is never opened. Absent, the done-state rule is skipped rather than guessed."
        }
      },
      "additionalProperties": false
    },
    "handoffImpact": {
      "type": "object",
      "description": "Reviewed exact-file exceptions for the Layer 2 content-drift gate. Only a modification (git status M) of a listed regular tracked file is non-impacting; additions, deletions, renames, copies, type changes, and mixed source changes remain impacting.",
      "required": [
        "nonImpactingModifiedFiles"
      ],
      "properties": {
        "nonImpactingModifiedFiles": {
          "type": "array",
          "description": "Exact repo-relative regular tracked files whose content-only modification does not change handoff state. Runtime validation rejects duplicates and prefix-like ambiguity and rejects directories, symlinks, and gitlinks.",
          "items": {
            "type": "object",
            "required": [
              "file",
              "reason"
            ],
            "properties": {
              "file": {
                "type": "string",
                "minLength": 1,
                "pattern": "^[A-Za-z0-9._@+ -]+(?:/[A-Za-z0-9._@+ -]+)*$",
                "not": {
                  "anyOf": [
                    { "pattern": "^aahp\\.config\\.json$" },
                    { "pattern": "^\\.ai/handoff(?:/|$)" },
                    { "pattern": "(?:^|/)\\.{1,2}(?:/|$)" }
                  ]
                },
                "description": "Exact repo-relative regular tracked file. Forward slashes only; no absolute paths, traversal, glob syntax, directories, symlinks, gitlinks, handoff files, or aahp.config.json."
              },
              "reason": {
                "type": "string",
                "minLength": 1,
                "pattern": "[\\p{L}\\p{N}]",
                "not": {
                  "pattern": "[\\p{Cc}\\p{Cf}]"
                },
                "description": "Review rationale explaining why modifying this file cannot stale handoff state. It must contain a Unicode letter or number and cannot contain control or invisible format characters."
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "check": {
      "type": "object",
      "description": "Selects which gates `aahp check` runs. Both lists are optional; omit to run every applicable gate. Gate ids: changelog, changelog-format, version-sync, claims, forbidden-patterns, schema-doc-sync, doc-links, handoff.",
      "properties": {
        "only": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Run only these gate ids (whitelist)."
        },
        "skip": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Skip these gate ids (blacklist), applied after only."
        }
      },
      "additionalProperties": false
    },
    "pinnedDep": {
      "type": "object",
      "description": "Opt-in distribution-pin assertion for the aahp doctor pinned-dep gate. Absent: the gate reports skip. A repo whose own package name equals name reports self.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Package that must be pinned (default @elvatis_com/aahp)."
        },
        "location": {
          "type": "string",
          "enum": [
            "devDependencies",
            "dependencies",
            "any"
          ],
          "description": "Which dependency block must carry the pin (default devDependencies)."
        },
        "allowRange": {
          "type": "boolean",
          "description": "If true, accept a semver range; default false requires an exact pin."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
